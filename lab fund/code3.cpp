#include <bits/stdc++.h>
void q()
{
    int n, a[100], val;
    scanf("%d%d", &n, &val);
    for (int i = 1; i <= n; i++)
        scanf("%d", &a[i]);
    for (int i = 1; i <= n; i++)
        if (a[i] == val)
        {
            printf("%d ", i );
        }
}
int main()
{
   unsign
}