#include <bits/stdc++.h>

#define ll long long
#define pp pair<int, int>

const int N = 1000;

using namespace std;
bool fee[N][N], cannot[N][N];
int n, e, k;
ll res = 0;
signed main() {
    ios_base::sync_with_stdio(false), cin.tie(NULL);
    cin >> n >> e >> k;
    
}
