#include <bits/stdc++.h>
#define maxn 21
#define ll long long
#define pp pair<int, int>
#define fastio ios_base::sync_with_stdio(false), cin.tie(NULL);
using namespace std;
int main()
{
    int testcase = 0, x, y, z;
    ll a[maxn][maxn][maxn], cube[maxn][maxn][maxn];
    cin >> x >> y >> z;
    for (int i = 1; i <= x; i++)
        for (int j = 1; j <= y; j++)
            for (int k = 1; k <= z; k++)
            {
                cin >> a[i][j][k];
            }
}