#include <bits/stdc++.h>
#define ll long long
#define pp pair<int, int>
using namespace std;
const int N = 1000;
int a[20], n;
signed main() {
    ios_base:: sync_with_stdio(false), cin.tie(NULL);
    int temp;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> temp;
        a[temp++];
    }
    for (int i = 0; i <= 10; i++)
        for (int j = 0; j < i; j++) {
            string temp = i + '0' + j;
        }
}
