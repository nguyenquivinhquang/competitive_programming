#include <bits/stdc++.h>

#define ll long long
#define pp pair<int, int>

const int N = 1000;

using namespace std;

double f_sin(double x) {
    return 0.5 * sin((x + 10.0) / 110.0);
}
double linear(double x) {
    return  x*x + x + 1;
} 
signed main() {
    ios_base::sync_with_stdio(false), cin.tie(NULL);
    
}

/*
 Don't use endl, use '\n'
 just use endl with interactive problems !!


*/